import rabbit from "../lib/rabbit.js"
import S3Handler from "@quizify/s3-handler"
import type { S3HandlerConstructor } from "@quizify/s3-handler"
import type { Request, Response } from "express"
import { services, env, queues } from "../util.js"

export const test = () => {
  
}